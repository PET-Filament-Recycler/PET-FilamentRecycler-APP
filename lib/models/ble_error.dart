/// BLE error codes emitted by [BleService] for UI localization.
enum BleErrorCode {
  scanError,
  fallbackScanError,
  connectionFailed,
  connectionLost,
  notConnected,
  sendFailed,
  bluetoothOff,
}